{
  "name": "mgrnz-blog",
  "version": "1.0.0",
  "description": "Hugo blog with admin system",
  "scripts": {
    "build": "hugo --gc --minify",
    "dev": "hugo server",
    "clean": "rm -rf public"
  },
  "engines": {
    "node": "18"
  },
  "devDependencies": {
    "autoprefixer": "^10.4.19",
    "postcss-cli": "^11.0.0",
    "tailwindcss": "^3.4.3"
  }
}